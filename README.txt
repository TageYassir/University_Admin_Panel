You need to install a sql database worbench and create the following tables.

// login (make sure to insert a values of login manually using sql code). (username,password).
// student. (name,fname,regestration,dob,address,phone,email).
// studentleave. (regestration,name,fname,branch,leave_date).
// teacher. (name,fname,regestration,dob,address,phone,email).
// teacherleave. (regestration,name,fname,branch,leave_date).
// subject_marks. (regestration,semester,subbject_name,marks).
// fee_status. (regestration,branch,semester,is_paid).
// fee. (branch,semester1,semester2,semester3,semester4,semester5,semester6,semester7,semester8,semester9,semester10).
// collegefee (For this one also make sure to enter values manually). (regestration,branch,semester,total).

